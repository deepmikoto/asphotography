/*!
 * andreeasanduloiu.ro app start
 */

/** initialize app variable */
var asphotography = {};

/** define bootstrapped data */
var root = root || '/';

/** define api routes */
asphotography.apiRoutes = {
    FETCH_TEMPLATES_URL             : '_api/templates'
};

/** define the templates variable */
asphotography.templates = {};